[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9.15.0", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @masjid/database run generate",
  "cd apps/api && pnpm exec tsc"
]

[start]
cmd = "node apps/api/dist/server.js"
